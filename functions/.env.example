# Copy this file to functions/.env and fill in your values.
# Owner free-create code: visit /create?owner=THIS_VALUE to make books for free.
OWNER_CODE=change-me

# Admin allowlist for the /console moderation view (comma-separated Google emails).
# These accounts can list and delete ANY book. Empty = nobody (console denies all).
ADMIN_EMAILS=you@example.com
